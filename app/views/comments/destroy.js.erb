$("#destroy_comment_"+<%=@comment.id%>).fadeOut(300, function() { $(this).remove(); });
$("#new_comment_"+<%=@comment.id%>).fadeOut(300, function() { $(this).remove(); });
$(".comment_head_left").html('COMMENTS (<%=@article.comments.count %>)');
$("#fav_count").html('<%=@article.comments.count %>');
console.log(<%=@comment.id%>);
